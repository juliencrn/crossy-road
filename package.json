{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "lint": "prettier --write \"src/**/*.js\" && standard \"src/**/*.js\" --fix"
  },
  "dependencies": {
    "canvas-confetti": "^1.2.0",
    "standard": "^16.0.3"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "snowpack": "^2.18.3"
  }
}
